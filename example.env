APP_ID=
VITE_UNSPLASH_ACCESS_KEY=
SECRET_KEY=
# Used only by scripts/vocab:generate-images (Node-side, never bundled to the client)
OPENAI_API_KEY=
# Supabase — sync de progreso entre dispositivos (docs/BACKLOG.md #15).
# Usar la Publishable key (sb_publishable_...), nunca la Secret key aquí.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=